// This file is intentionally created to showcase JavaScript usage for GitHub language statistics.
// Example: Tailwind + JS interactivity
function showTailwindInfo() {
  const info = document.createElement('div');
  info.className = 'example';
  info.innerText = 'Tailwind CSS and JavaScript are both used in this project!';
  document.body.appendChild(info);
}
// Uncomment to run in a browser environment
// showTailwindInfo();
